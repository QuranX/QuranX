@using (Html.BeginForm(
        "Index",
        "Search",
        method: FormMethod.Get,
        htmlAttributes: new
        {
            id = "inlineSearch"
        }
    ))
{
    @Html.TextBox("q", "", new { type = "search", results = 10, name = "inlineSearch", placeholder = "Quick search..." })
}